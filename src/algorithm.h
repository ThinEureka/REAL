#pragma once

#ifndef ALGORITHM_H
#define ALGORITHM_H

namespace zju04nycs {
	class INT;
	// m and n should be greater than zero
	INT& gcd(const INT& m, const INT& n, INT& gcd, INT& c1, INT& c2, INT& c3);
}

#endif
